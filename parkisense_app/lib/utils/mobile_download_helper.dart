import 'dart:io';
import 'dart:convert';

class WebDownloadHelper {
  static void downloadFile(String content, String fileName) {
    // This is a stub for mobile - the actual implementation is in the calling code
    throw UnimplementedError('Web download not available on mobile');
  }
}
